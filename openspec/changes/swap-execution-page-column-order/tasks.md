# Tasks: 运维执行页左右两栏顺序交换

## 1. 第一行左右交换

- [x] 1.1 第一行：左侧 Col 改为渲染 ExecutionOptions，右侧 Col 改为渲染 ExecutionModeSelector（与当前顺序对调）

## 2. 第二行左右交换

- [x] 2.1 第二行：左侧 Col 改为渲染 HostSelector，右侧 Col 改为渲染 ScriptEditor（与当前顺序对调）

## 3. 验收

- [x] 3.1 桌面端并排时，第一行左侧为执行选项、右侧为模式选择；第二行左侧为主机选择、右侧为脚本编辑
- [x] 3.2 窄屏上下堆叠时，顺序与设计一致（先左后右即先执行选项/主机选择，再模式选择/脚本编辑）
