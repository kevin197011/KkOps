## 1. Implementation

- [x] Add `CMDBAsset` and `AssetRelation` models + AutoMigrate
- [x] CMDB service + handler (assets CRUD, relations list/create/delete)
- [x] Topology graph endpoint + handler
- [x] Frontend `/cmdb` and `/topology` pages + API clients + RBAC `cmdb:*`
- [x] `openspec validate add-cmdb-topology --strict`
