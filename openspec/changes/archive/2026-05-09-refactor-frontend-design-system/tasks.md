## 1. Shell primitives
- [x] 1.1 `PageContainer` — page-level wrapper with consistent padding/max-width.
- [x] 1.2 `PageHeader` — title + description + actions slot.
- [x] 1.3 `EmptyState` — illustrated empty state with optional CTA.
- [x] 1.4 `Section` — labelled section block.

## 2. Command palette
- [x] 2.1 `CommandPalette` component (CMD/CTRL + K) listing routes filtered by permissions.
- [x] 2.2 Mount in `MainLayout` and bind global keyboard shortcut.

## 3. Spec
- [x] 3.1 Add `frontend-design-system` capability spec.
- [x] 3.2 `openspec validate refactor-frontend-design-system --strict` passes.
