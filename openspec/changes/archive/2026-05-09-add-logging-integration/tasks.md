## 1. Backend

- [x] 1.1 `internal/integration/logging`: Loki + Elasticsearch clients, `LogLine` model.
- [x] 1.2 `POST /api/v1/logging/search` with `logging:*`.

## 2. Frontend

- [x] 2.1 `LogSearch.tsx` with search form and scrollable results list.

## 3. Spec

- [x] 3.1 Capability `log-search` spec.
- [x] 3.2 `openspec validate add-logging-integration --strict` passes.
